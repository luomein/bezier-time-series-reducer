import XCTest
@testable import bezier_time_series_reducer

final class bezier_time_series_reducerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(bezier_time_series_reducer().text, "Hello, World!")
    }
}
